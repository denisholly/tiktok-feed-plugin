import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tiktok-feed-plugin',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
